# Application
spring.application.name=FurniHub

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/e_commerce?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Robin@Singh@755321
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# SQL Initialization (optional - for fresh DB setup)
spring.sql.init.mode=never
spring.sql.init.schema-locations=classpath:schema.sql

# JWT
jwt.secret=FurniHubSecretKeyForJWTTokenGeneration2024SecureRandomValue
jwt.expiration=2592000000

# Server
server.port=8080

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Razorpay (Test Mode)
razorpay.key-id=rzp_test_TLJ4wDlknTTeqx
razorpay.key-secret=OaxbN5tf8PlOXaNG0WZ8FduM
razorpay.mode=test

# Cookie Configuration
app.cookie.name=auth_token
app.cookie.max-age=0
app.cookie.path=/
app.cookie.http-only=true
app.cookie.secure=false
app.cookie.same-site=Lax

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.furnihub=DEBUG
